import { Text, View } from "react-native";

const StoryCard = ({ id, title, cover_img }: Story) => {
  return (
    <View>
      <Text className="text-white text-sm">StoryCard</Text>
    </View>
  );
};

export default StoryCard;
